const fs = require("fs"); const files = fs.readdirSync(".").filter(file => file.endsWith("Shader.js")); files.forEach(file => { const content = fs.readFileSync(file, "utf8"); const updated = content.replace("import \"./shader-component.js\";", "import { ShaderComponent } from \"./shader-component.js\";"); if (content !== updated) { fs.writeFileSync(file, updated, "utf8"); console.log(`Updated ${file}`); } });
